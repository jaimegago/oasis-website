# oasis-website

Hugo source for the OASIS specification website (oasis-spec.dev).

## What this repository is

A Hugo site using the Hugo Book theme that renders the OASIS specification as a documentation website. The theme was chosen because it is a clean, single-CSS-file documentation theme that is straightforward for LLM-assisted maintenance — no complex JavaScript build pipeline, no framework abstractions.

## Relationship to oasis-spec

The specification content lives in a separate repository at github.com/jaimegago/oasis-spec. Content is pulled in at build time by a Go build script. This repository never modifies spec content — it only reads and renders it. The oasis-spec repository is the single source of truth for all specification text.

## Build script

The build script will live at `cmd/oasis-site-build/` (to be created in a later prompt). It is a Go program that fetches spec content from the oasis-spec repository and places it under `content/en/docs/` for Hugo to render.

## Launch blockers

- **conduct@oasis-spec.dev email**: The Code of Conduct page references conduct@oasis-spec.dev as the reporting email. This email address does not exist yet and must be created before the site goes public.
- **Governance document**: The current governance page is an intentional stub. A full governance document (steering committee, RFC process, voting procedures) must be written before the v1.0 release.

## Known invariants

These rules must not be broken by any future session:

- **Single source of truth**: The oasis-spec repository (github.com/jaimegago/oasis-spec) is the sole source of specification content. Never hand-edit generated content under `content/en/docs/`.
- **No manual spec edits**: Content under `content/en/docs/` is generated by the build script. Manual changes will be overwritten.
- **No extra JavaScript**: The site must not include any JavaScript beyond what the Hugo Book theme ships with.
- **Hugo Modules for theme**: The theme is installed via Hugo Modules, not git submodules or vendored copies.
- **Self-hosted fonts**: IBM Plex Sans and JetBrains Mono are downloaded by `scripts/fetch-fonts.sh` into `static/fonts/` (gitignored). No runtime requests to external font services.
- **Use design tokens**: All colors must come from `--oasis-*` CSS custom properties. Never hard-code hex color values in stylesheets. All spacing uses `--space-*` tokens; all border-radius uses `--radius-*` tokens.

## Visual identity

### Typography rationale

**IBM Plex Sans** is the body and UI font. It was chosen for its open-source license, excellent legibility at small sizes, and professional tone that matches technical specification documents. It is the typeface IBM designed for technical communication, making it a natural fit for a standards body site.

**JetBrains Mono** is the code and technical metadata font. It is used for all inline code, code blocks, version badges, and scenario identifiers. It was chosen for its programming-focused design with distinguishable characters (0/O, 1/l/I) and ligature-free readability.

Both fonts are self-hosted as woff2 under `static/fonts/` (gitignored, fetched by `scripts/fetch-fonts.sh`). Font declarations use `font-display: swap` with system font fallback stacks.

### Accent color rationale

**Deep teal (#0F5B5E)** was chosen as the accent color from a swatch comparison. It is serious and professional without being corporate-blue, distinctive enough to be recognizable, and passes WCAG AA contrast requirements against both the near-white light background and the near-black dark background. The lighter teal variant (#5DCAA5) for dark mode maintains visual identity while meeting contrast requirements against dark backgrounds.

### Color tokens

All colors are defined as CSS custom properties with the `--oasis-` prefix in `assets/_custom.scss`.

#### Light mode (default)

| Token | Value | Usage |
|-------|-------|-------|
| `--oasis-bg` | `#FAFAF9` | Page background (near-white, slightly warm) |
| `--oasis-bg-elevated` | `#FFFFFF` | Cards, dropdowns, table headers |
| `--oasis-text` | `#1A1A1A` | Primary text |
| `--oasis-text-secondary` | `#555555` | Sidebar section headers, secondary labels |
| `--oasis-text-tertiary` | `#888888` | Hints, line numbers |
| `--oasis-border` | `#E5E5E3` | Default borders |
| `--oasis-border-strong` | `#CFCFCD` | Table header bottom border |
| `--oasis-accent` | `#0F5B5E` | Links, active states, buttons |
| `--oasis-accent-hover` | `#0A4548` | Hover states |
| `--oasis-code-bg` | `#F4F4F2` | Code block and inline code background |

#### Dark mode (`@media (prefers-color-scheme: dark)` and `[data-theme="dark"]`)

| Token | Value | Usage |
|-------|-------|-------|
| `--oasis-bg` | `#0F0F0E` | Page background |
| `--oasis-bg-elevated` | `#1A1A19` | Cards, dropdowns, table headers |
| `--oasis-text` | `#F0F0EE` | Primary text |
| `--oasis-text-secondary` | `#AAAAA8` | Secondary labels |
| `--oasis-text-tertiary` | `#777775` | Hints, line numbers |
| `--oasis-border` | `#2A2A28` | Default borders |
| `--oasis-border-strong` | `#3F3F3D` | Table header bottom border |
| `--oasis-accent` | `#5DCAA5` | Links, active states, buttons |
| `--oasis-accent-hover` | `#7FD9B9` | Hover states |
| `--oasis-code-bg` | `#1F1F1D` | Code block and inline code background |

### Spacing tokens

`--space-1` (0.25rem) through `--space-24` (6rem). See `assets/_custom.scss` for the full set.

### Radius tokens

| Token | Value |
|-------|-------|
| `--radius-sm` | `4px` |
| `--radius-md` | `6px` |
| `--radius-lg` | `10px` |

### Syntax highlighting

Chroma styles: **trac** for light mode (neutral, no competing colors), **native** for dark mode (muted colors readable on dark backgrounds). Both are embedded directly in `assets/_custom.scss` as CSS class rules (Hugo is configured with `noClasses: false` in `hugo.yaml`). The Chroma background colors are overridden to use `--oasis-code-bg` so they match the rest of the palette.

### Custom stylesheet location

All visual identity styles live in `assets/_custom.scss`. This is Hugo Book's documented extension point — the theme's `book.scss` imports `_custom.scss` at the end, so our styles override theme defaults. The file is organized in numbered sections:

1. Font-face declarations
2. Spacing and radius tokens
3. Light mode color tokens
4. Dark mode color tokens
5. Theme mixin overrides (maps OASIS tokens to Hugo Book's internal variables)
6. Typography and type scale
7. Links
8. Buttons
9. Code blocks and inline code
10. Tables
11. Sidebar and navigation
12. Table of contents
13. Search
14. Blockquotes
15. Header
16. Version dropdown
17. Footer
18. Border radius consistency
19. Technical metadata classes
